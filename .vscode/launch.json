{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "go",
            "request": "launch",
            "name": "Run Debug",
            "mode": "auto",
            "program": "${workspaceFolder}"
        }
    ]
}